fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
# Windows ships no system timezone database; Linux images usually do, but
# pinning it keeps seed times identical on both.
tzdata==2024.2

# dev / test only
pytest==8.3.4
httpx==0.28.1
